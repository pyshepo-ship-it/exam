# التقرير النهائي — المراجعة البرمجية والأمنية الشاملة + اختبار كل وحدة

**المشروع**: نظام إدارة أكاديمي (Next.js 16 + TypeScript + Supabase) — مسار `/home/user/exam`
**الفرع**: `arena/01a06896-exam`
**تاريخ الإنجاز**: 2026-09-04

---

## 1) الملخص التنفيذي

- أُجريت مراجعة برمجية وأمنية لكل الملفات الحسّاسة سطراً سطراً (محرك المزامنة، طبقة البيانات، حسابات البوابة، المخطط وقواعد RLS، كل الترحيلات، لوحات الحذف/الطلاب/الصفوف)، مع مسح أمني آلي شامل لكل سطر في باقي المشروع (~24.5 ألف سطر) للأنماط الخطرة (XSS، تسريب مفاتيح، eval، innerHTML، روابط خارجية، قيم Radix فارغة…).
- **كل الاختبارات خضراء 100%**: `tsc`، `eslint` (0 أخطاء)، `next build`، `npm run verify` كاملاً، وكل سكربتات الفحص في `scripts/` (تفاصيل في القسم 6).
- **أُصلح في هذه الجولة**: إزالة بصمة FNV الضعيفة من مسار كتابة كلمات المرور، حذف ملف Middleware ميت، حذف متسلسل (Cascade) في ذاكرة الجلسة يطابق قيود قاعدة البيانات عند حذف صف/مجموعة/طالب، نصوص تأكيد صادقة مطابقة لأثر الحذف الفعلي، ترحيل تحصين `014_harden_anon_reads.sql`، وإضافة 29 فحصاً جديداً (6 لسياسات RLS + 23 للحذف المتسلسل) إلى `fresh-audit-suite` (أصبح 81 فحصاً).
- **الثغرات البنيوية الثلاث المعروفة سابقاً**: جرى التحقق منها وتوثيقها مع «الحل المعتمد» الكامل (القسم 4) — لا يمكن إغلاقها نهائياً من SQL وحده لأن جذرها أن بوابة الطالب تعمل بلا مصادقة Supabase حقيقية.

---

## 2) نطاق المراجعة ومنهجيتها

### قراءة سطراً بسطر (هذه الجولة والجولات السابقة)
- `src/lib/supabase/sync.ts` (2064 سطراً) — **مكتملة**: الخرائط، pushRows والطابور، كل دوال الدفع، السحب، مسارات الزوار/البوابة، مسح الكل، التشخيص، forcePushAll، diagnoseSync.
- `src/lib/data-storage.ts` — **مكتملة** (بما فيها دوال الحذف المتسلسل الجديدة في نهايتها).
- `src/lib/student-accounts.ts` — **مكتملة** (التسجيل، الموافقة، النقل، التعديل، إعادة التعيين، الاسترجاع، الحذف، جلسة الطالب).
- `supabase/schema.sql` (308) + الترحيلات `001…013` — قراءة الأجزاء الحاسمة والمسح الكامل للقيود، و`010/011/012/013` كاملة.
- لوحات `dashboard/grades`، `dashboard/students` (مقاطع الحذف والإدارة)، `dashboard/layout`، `exam/[id]` (المنطق)، `dashboard/payments` (الهيكل).

### مسح أمني آلي شامل لكل ملف في src (لا يُستثنى أي سطر)
مطبَّق ضمن `npm run audit` و`system-deep-audit` و`cloud-only-audit` و`fresh-audit-suite` على **كل ملف**: غياب `dangerouslySetInnerHTML`/eval/دوال التخزين المحلي القديمة، لا تسريب `service_role`، لا روابط بلا `rel="noopener noreferrer"`، لا قيم فارغة في Radix `SelectItem`، وغياب `innerHTML` إلا في مولّدي الطباعة التي تهرب كل المدخلات عبر `esc()` (تحققت منها يدوياً في `schedule-print` و`student-report`).

> ملاحظة أمانة: عدد من صفحات العرض الكبيرة (لوحات المحتوى العام، تقارير، طلبات…) غُطّي عبر المسح الآلي الشامل أعلاه + اختبارات الوحدات، ويُفضَّل استكمال قراءتها سطراً بسطر في مراجعة لاحقة — لا توجد بها أنماط خطرة بحسب الفحص الآلي.

---

## 3) ما أُصلح في هذه الجولة (مُختبر)

### أ) كلمات المرور — إزالة الـ fallback الضعيف (`src/lib/student-accounts.ts`)
- `sha256Hex` كانت تسقط بصمت إلى بصمة FNV-1a غير آمنة عند غياب WebCrypto. الآن: **ترمي خطأً برسالة واضحة**، وكل كتابة بصمة جديدة هي SHA-256 فقط.
- FNV القديم تحول إلى `legacyFnvHex` للقراءة فقط + كاشف `isLegacyFnvHash` + دالة `passwordMatches` (المصدرة): الحسابات القديمة تستطيع **الدخول فقط** ولا تُكتب بصمتها الضعيفة مجدداً — لا تخفيض لأمان الحسابات الجديدة ولا كسر لدخول القدامى.
- `try/catch` برسالة مستخدم في المواضع الثلاثة: تسجيل الطالب، إعادة التعيين من المعلم، ومقارنة الدخول.

### ب) حذف ملف ميت
- `src/lib/supabase/middleware.ts` حُذف (`git rm`) — لم يكن مستورداً في أي مكان؛ الحارس الفعلي هو `src/proxy.ts` (يستخدم `getUser()` الصارم ويحمي `/dashboard`).

### ج) الحذف المتسلسل — إصلاح «البيانات اليتيمة» عند حذف صف/مجموعة/طالب
- **المشكلة**: كانت لوحة الحذف تزيل الصف من الذاكرة فقط، فتبقى المجموعات/الطلاب/المال/الحضور داخل الجلسة، ويستعيد «السحب التالي» الصف المحذوف من السحابة بعد رفعه لاحقاً.
- **الإصلاح** (في نهاية `src/lib/data-storage.ts`): دوال `deleteGradeCascade` / `deleteGroupCascade` / `deleteStudentCascade` تعكس **بالضبط** قيود المخطط:
  - حذف صف ← تُحذف مجموعاته وحصص مجموعاته وحضورها (CASCADE)، ويبقى طلابه بلا صف/مجموعة واختباراته واستحقاقاته بلا مرجع (SET NULL).
  - حذف مجموعة ← حصصها وحضورها تُحذف، وطلابها يبقون داخل صفهم بلا مجموعة.
  - حذف طالب ← تُحذف استحقاقاته ومدفوعاته وحضوره ودرجاته اليدوية وسجل نشاطه وحساب بوابته وطلبات نقله (CASCADE)، ويبقى طلب تسجيله المعتمد بلا ربط (SET NULL)، وتبقى محاولات اختباراته وأوسمة شرفه (بلا FK في المخطط — سجل تاريخي).
- لوحتا `grades` و`students` تستخدمان الدوال الجديدة مع **نصوص تأكيد صادقة** تُخبر المعلم بالأثر الحقيقي على القاعدة.

### د) التحصين SQL (`supabase/migrations/014_harden_anon_reads.sql`) — جديد
- يسحب قراءة `anon` (SELECT + سياسة `public read`) من `sessions` و`year_archives` — الجدولان الوحيدان بلا أي مستهلك مجهول في الكود.
- سحب وقائي صريح لأي `UPDATE/DELETE` من `anon` على **كل** الجداول.
- يوثّق داخله «الحل المعتمد» الكامل للمخاطر البنيوية المتبقية (القسم 4).

### هـ) حارس تراجع RLS + اختبارات الحذف (`scripts/fresh-audit-suite.mjs`)
- بند 7 جديد يفحص ملفات الترحيل: يمنع مستقبلاً منح `anon` قراءة الجداول المحمية، ويتأكد أن 014 نُفّذ (REVOKE + DROP POLICY).
- قسم 6.5 جديد: 23 فحصاً وظيفياً لدوال الحذف المتسلسل (بلا يتامى، مطابقة لقيود القاعدة) — كشف فعلياً خطأً (المجموعات لم تكن تُحذف من الصف الباقي) ثم أُصلح.
- **النتيجة: 81/81 فحصاً** (كانت 52/52).

---

## 4) الثغرات الثلاث الموثقة سابقاً — التحقق والحل المعتمد

### 1) قراءة anon الشاملة للجداول الحساسة (من 010 ثم 012/013) — **مؤكدة**
- `010_repair_align.sql` يمنح anon `SELECT` عبر سياسة `public read … USING (true)` + `GRANT SELECT` على: `students` (أسماء/هواتف/بريد)، `dues`/`payments` (مال)، `attendance`، `manual_grades`، `student_history`، `student_accounts`/`registration_requests` (بريد + بصمة SHA-256)، `inquiries` (مراسلات)، `exams` (كل الاختبارات بأسئلتها)، `exam_attempts` (إجابات ودرجات الطلاب). و012/013 لم يسحبا ذلك بل أكدا SELECT للزائر.
- **السبب الجذري**: بوابة الطالب تقرأ بياناتها عبر مفتاح anon بنمط «اجلب الكل ثم فلتر جهة العميل» (`fetchStudentPortalData` تطلب 13 جدولاً كاملاً وتفلتر بـ studentId في المتصفح) — فلا يمكن تضييق الصفوف في RLS دون كسر الوظيفة، ولا توجد هوية موثوقة (JWT) لربط الصف بالمستخدم.
- **حالة الآن**: ثغرة مفتوحة **موثقة بحل معتمد** + تخفيف آمن مطبّق (014).

### 2) كوكي جلسة الطالب غير موقّع وقابل للتزوير — **مؤكدة**
- `student-accounts.ts` يخزن الجلسة في كوكي `studentPortalSession` = JSON (email/studentId/name/iat/exp) مبسوطة base64 فقط بلا توقيع — أي زائر يعدّل studentId ويغيّر exp يستطيع الانتحال. التخفيف الوحيد أن بيانات البوابة كلها مكشوفة أصلاً عبر (1) — فالتزوير لا يزيد الكشف.
- **حالة الآن**: موثقة — تُحل نهائياً بجلسات Supabase الحقيقية (JWT) في المرحلة (أ/ب) أدناه.

### 3) أسئلة/إجابات الاختبارات مكشوفة (ختم base64/FNV تشويشي فقط) — **مؤكدة**
- التصحيح **كله جهة العميل**: `exam-grade.ts` يقرأ `choice.isCorrect` / `sq.isTrue` / `sq.correctAnswer` / `corrections[].correctAnswer` من questions JSONB. النسخة المعروضة للطالب تُجرّد الإجابات عبر `stripExamAnswers`/`sealExamForStudent`، لكن: (أ) مفاتيح التصحيح محمولة داخل الـ seal في المتصفح وقابلة للفك لأن التصحيح نفسه يتم هناك، و(ب) جدول `exams` في القاعدة يخزن الإجابات كاملة ويقرؤه anon بالكامل (بند 1) — فالكشف لا يتطلب حتى فك ختم.
- **حالة الآن**: موثقة — الحل النهائي تصحيح من جهة الخادم (RPC) كما في المرحلة (ج) أدناه.

### الحل المعتمد (Approved Solution — موثّق كاملاً داخل 014)
| المرحلة | الإجراء | يغلق |
|---|---|---|
| أ | نقل دخول الطلاب إلى Supabase Auth عند موافقة المعلم (`supabase.auth.admin.createUser` + metadata.student_id من بيئة الخادم فقط) | 2 (جلسات JWT غير قابلة للتزوير) |
| ب | سياسات RLS بشرط `student_id = …(auth.jwt())` لكل جدول بوابة، ورفع SELECT نهائياً عن anon من الجداول الحساسة | 1 |
| ج | RPC `submit_exam_attempt(exam_id, answers)` يصحّح داخل الخادم دون إرسال `isCorrect`؛ وعرض عام آمن لجدول exams | 3 |
| د | تخفيفات فورية لا تكسر الوظيفة (طُبّق 014: sessions + year_archives) | 1 جزئياً |

---

## 5) ملاحظات إضافية رُصدت في المراجعة

- **البنية «سحابة خالصة» سليمة**: لا تخزين محلي للبيانات إطلاقاً؛ الذاكرة مؤقتة والجلب عند كل فتح، ومسارات الإرسال (تسجيل/نقل/استفسار) تكتب في Supabase **أولاً** ثم تُحدّث الذاكرة.
- **الترتيب عند الرفع سليم**: `pushRows` ترفع صفوف الجدول ثم تحذف المعرفات التي اختفت من آخر دفعة (هذه آلية نشر الحذف)، ودوال push تنظف/تسقط المراجع المعلّقة قبل الإرسال، و`queuePush` يعيد المحاولة كاملة عند خطأ FK.
- **RLS مدرّجة على كل الجداول** والسياسات متماسكة لدور authenticated (المعلم)، ولا يوجد `UPDATE/DELETE` للزائر في أي جدول (يؤكده سحب 014 الوقائي).
- **الـ views** بصلاحيات المتصل (security_invoker) بعد 011 — لا تجاوز لـ RLS.
- **فحص XSS**: لا `dangerouslySetInnerHTML` في المشروع إطلاقاً (يفرضه audit على كل ملف)، ومواضع `innerHTML` الثلاثة في معاينات الطباعة تبني HTML عبر مولّدات تهرب كل المدخلات بـ `esc()` (`schedule-print`/`student-report` تحققت منها)، و`pdf-utils` يصدّر عناصر DOM كصور (لا يبني HTML).
- **لا تسريب مفاتيح**: مفتاح `service_role` غير موجود في أي كود عميل/Middleware (فحص آلي)، و`proxy.ts` يستخدم `getUser()` لا `getSession()`.
- **حدود حماية معقولة**: حد 5 محاولات دخول/15 دقيقة، حد استرجاع 3/ساعة برسالة موحّدة بلا تسريب وجود الحساب، إعادة التعيين بكلمة مؤقتة 7 خانات وتُخزَّن بصمتها فقط.
- **المخطط متسق مع الواجهة**: FKs بين grades/groups/students/dues/payments/sessions/attendance وجداول البوابة كما يفترضها الكود (تحقق 014 من CASCADE/SET NULL مطابق).

---

## 6) نتائج الاختبارات الكاملة (كلها ✅)

| الفحص | الأمر | النتيجة |
|---|---|---|
| الأنواع | `npx tsc --noEmit` | ✅ بلا أخطاء |
| الـ lint | `npx eslint src scripts` | ✅ 0 أخطاء (تحذيرات `any` سابقة فقط) |
| البناء الإنتاجي | `npm run build` | ✅ 19 مساراً + Proxy |
| التحقق الكامل | `npm run verify` (tsc + cloud + build + كل السكربتات) | ✅ خروج 0 |
| فحص المخطط/الأعمدة | `npm run audit` | ✅ |
| منطق البيانات | `npm run test:logic` | ✅ |
| بوابة الطالب | `npm run test:portal` | ✅ |
| وضع فتح الاختبارات | `npm run test:exam-access` | ✅ |
| الجدول والطباعة | `npm run test:schedule` + `schedule-print` | ✅ |
| الختم ومنع التسريب | `npm run test:seal` | ✅ 8/8 |
| التدقيق العميق | `npm run test:deep` | ✅ 42/42 |
| السحابية الخالصة | `node scripts/cloud-only-audit.mjs` | ✅ 20/20 |
| **التدقيق الشامل الجديد** | `node scripts/fresh-audit-suite.mjs` | ✅ **81/81** |

---

## 7) الملفات المتغيّرة في هذه الجولة

- `src/lib/student-accounts.ts` — بصمة SHA-256 حصرية + passwordMatches + معالجة أخطاء WebCrypto.
- `src/lib/data-storage.ts` — دوال الحذف المتسلسل `deleteGradeCascade` / `deleteGroupCascade` / `deleteStudentCascade`.
- `src/app/dashboard/grades/page.tsx` و`students/page.tsx` — استخدام الحذف المتسلسل + نصوص تأكيد صادقة.
- `src/lib/supabase/middleware.ts` — حُذف (ميت).
- `supabase/migrations/014_harden_anon_reads.sql` — جديد (تحصين anon + توثيق الحل المعتمد).
- `scripts/fresh-audit-suite.mjs` — +29 فحصاً (RLS + الحذف المتسلسل) → 81/81.

## 8) توصيات للمرحلة التالية (بترتيب الأولوية)
1. تنفيذ المراحل أ–ج من «الحل المعتمد» (Supabase Auth للطلاب) — يغلق الثغرات 1 و2 و3 جذرياً.
2. نقل تصحيح الاختبارات إلى RPC على الخادم مع إبقاء `submitPublicAttempt` للإدراج فقط.
3. استكمال القراءة السطرية لصفحات العرض الكبيرة المتبقية (لوحات المحتوى/التقارير) — الفحص الآلي عليها نظيف.
